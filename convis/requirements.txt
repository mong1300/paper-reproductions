# Core
torch==2.13.0
torchvision==0.28.0
transformers==5.16.1
accelerate==1.14.0
safetensors==0.8.0
huggingface_hub==1.29.0

# Text-to-image
diffusers==0.40.0
compel==2.4.0

# Data / evaluation
pycocotools==2.0.11
pycocoevalcap==1.2
nltk==3.10.3
inflect==7.5.0          # replaces `pattern`, which does not run on Python 3.12

# Utilities
numpy==2.5.2
pillow==12.3.0
PyYAML==6.0.3
tqdm==4.70.0

# Optional — CUDA only, ignored on MPS
# bitsandbytes

# Optional — required only by eval/utils.py (LLaVA-Bench GPT review), unused here
# openai
